{% schema %}
  {
    "name": "Blog overview",
    "settings": [
      
    ],
    "blocks": [
      {
        "type": "blog",
        "name": "Blog",
        "settings": [
          {
            "type": "blog",
            "id": "blog",
            "label": "Blog"
          }          
        ]
      }
    ]
  }
{% endschema %}

{% for block in blocks %}
  <div class="blog">
  <h2>test</h2>
    <div class="blog-header">
      <h2>{{ block.blog.title }}</h2>
      <a href="{{ block.blog.url }}" style="text-transform:uppercase;">See all</a>
    </div>
    {% for article in block.blog.articles limit:3 %}
      {% render 'article-card', 
        blog: blog, 
        article: article, 
        show_image: true, 
        show_date: false, 
        show_author: false, 
        show_excerpt: false %}
    {% endfor %}
  </div>
{% endfor %}